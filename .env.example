# Copy this file to .env for local development.
# Render sets its own PORT automatically, so PORT is only needed locally.
PORT=3000

# PostgreSQL connection string, e.g.:
# postgres://username:password@localhost:5432/cse340
# On Render, use the "External Database URL" from your Postgres instance.
DATABASE_URL=
